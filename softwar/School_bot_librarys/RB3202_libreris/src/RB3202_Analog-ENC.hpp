#pragma once

#include <thread>
#include <mutex>
#include <Math.h>
#include <Arduino.h>
/*
class AnalogEncoder
{
    private:

    gpio_num_t pinA;
    gpio_num_t pinB;

    float korekceA1 = 0;
    float korekceB1 = 1;

    float korekceA2 = 0;
    float korekceB2 = 1;

    float pozition;
    int počet_otáček;

    esp_err_t sedPins(gpio_num_t pin0, gpio_nou_t pin1);
    public:

    AnalogEncoder(gpio_num_t pin0, gpio_nou_t pin1);

    int getPosition();

};*/